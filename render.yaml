services:
  - type: web
    name: spinnaker
    runtime: python
    buildCommand: echo "No build step needed"
    startCommand: python server.py
    envVars:
      - key: PORT
        value: 10000
    plan: free
